<% conversation.participants.each do |participant| %>
  <% unless participant == current_user %>
    <%= cl_image_tag (participant.gravatar.url), :width => 100, :height => 100, :crop => :fill, :gravity => :face %><br>
  <% end %>
<% end %>
