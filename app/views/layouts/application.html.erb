<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Down 2 Golf</title>
  <%= stylesheet_link_tag    "application" %>
  <%= include_gon %>
  <%= javascript_include_tag "application" %>
  <%= javascript_include_tag "jquery" %>
  <%= javascript_include_tag "chosen-jquery" %>
  <%= javascript_include_tag "jquery.reveal" %>
  <%= javascript_include_tag "foundation.min" %>
  <%= javascript_include_tag "jquery.raty" %>
  <%= javascript_include_tag 'ratyrate.js', "data-turbolinks-track" => false %>
  <%= javascript_include_tag "bodyscript" %>
  <%= csrf_meta_tags %>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/motion-ui/1.1.1/motion-ui.min.css" />
  <link href='https://cdnjs.cloudflare.com/ajax/libs/foundicons/3.0.0/foundation-icons.css' rel='stylesheet' type='text/css'>
</head>
<body>
  <div class="title-bar" data-responsive-toggle="main-menu" data-hide-for="medium">
    <button class="menu-icon" type="button" data-toggle></button>
    <% if user_signed_in? %>
      <div class="title-bar-title"><%=link_to current_user.email, current_user  %> </div>
    <% else %>
      <div class="title-bar-title">Menu</div>
    <% end %>
  </div>
  <div class="top-bar" id="main-menu">
    <div class="top-bar-left">
      <ul class="dropdown menu" data-dropdown-menu>
        <% if user_signed_in? %>
          <li class="menu-text"><i class="fi-skull"> <%=link_to current_user.email, current_user  %> </i></li>
        <% else %>
          <li class="menu-text"><i class="fi-skull">  Down 2 Golf? </i></li>
        <% end %>
      </ul>
    </div>
    <div class="top-bar-right">
      <ul class="menu" data-responsive-menu="drilldown medium-dropdown">
        <% if current_user %>
          <% if !current_user.premium %>
            <li> <%=link_to "Premium", new_checkout_path %></li>
          <% end %>
        <% end %>
        <% if user_signed_in? %>
          <li> <%= link_to "Courses", root_path %> </li>
          <li> <%= link_to "Matches", matches_path %> </li>
          <li><%= link_to 'Edit Profile', edit_user_registration_path %></li>
          <li><%= link_to 'Messages', conversations_path %></li>
          <li><%= link_to 'Log Out', logout_path%></li>
        <% else %>
          <li> <%= link_to "Sign up", signup_path %> </li>
          <li><%= link_to 'Log In', new_user_session_path %></li>
        <% end %>
      </li>
    </ul>
  </div>
</div>
  <%= render partial: "layouts/notifications" %>
  </header>
  <div class="row pad-down">
    <div class="large-6 medium-6 columns large-centered medium-centered">
      <% if notice %>
        <div class="success callout" data-closable="slide-out-right">
          <p class="notice text-center"><i class="fi-check"></i> <%= notice %></p>
          <button class="close-button" aria-label="Dismiss alert" type="button" data-close>
          <span aria-hidden="true">&times;</span></button>
        </div>
      <% end %>
      <% if alert %>
        <div class="alert callout" data-closable="slide-out-right">
          <p class="alert text-center"><i class="fi-alert"></i> <%= alert %></p>
          <button class="close-button" aria-label="Dismiss alert" type="button" data-close>
            <span class="xbtn" aria-hidden="true">&times;</span></button>
        </div>
      <% end %>
    </div>
  </div>

<%= yield %>

</body>
</html>
