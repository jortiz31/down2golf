<% @courses.each do |course| %>
<%=link_to course.name, course_show_path(course) %><br>
<%= course.location %><br>
<%= course.price %><br>
<%= course.url %><br>
<img src= "<%=course.image %>">
<% end %>
